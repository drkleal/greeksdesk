import {connectorRequest} from './connector.mjs';
import {createUpdateLoop} from './update-loop.mjs';
import {startChartShare,resizeChart,listReads,saveRead,loadESDraft,saveESDraft,chartImageBlob} from './capture.mjs';
import {node,renderMap,sourceLink} from './map.mjs';
import {renderEvidence,showEvidenceTab,focusEvidence} from './evidence.mjs';
import {levelDetails,priceText,directionTitle,setupRoom} from './plan.mjs';
import {today,initialSession,validDate} from './session.mjs';
const $=id=>document.getElementById(id);
let savedSession;try{savedSession=JSON.parse(localStorage.getItem('greeksdesk-session'));}catch{}
$('session').value=initialSession(savedSession);
$('map-unit').textContent=$('instrument').value;
function rememberSession(){try{localStorage.setItem('greeksdesk-session',JSON.stringify({date:$('session').value,savedOn:today()}));}catch{}}
rememberSession();
let connectorEnabled=false;
let sources=[],charts=[],read=null,share=null,busy=false,revision=0,cycles=0,analyses=0,manualMode=null,lastUpdateWarnings=[];
const links={quantdata:'https://v3.quantdata.us/page/custom/4e4699d7-e026-4cd8-9f43-24c832d25857',gamma:'https://app.optionsdepth.com/dashboard?tab=table'};
const message=t=>$('status').textContent=t;
function lock(value){busy=value;for(const id of ['update','analyze','session','instrument','basis','include-gamma','auto-analysis','upload','share','connect-charts','es-image','confirm-es','es-price-time','retry-basis','analyze-es'])$(id).disabled=value;}
function usage(){$('usage').textContent=`This tab: ${cycles} update cycles · ${analyses} analysis requests. Provider charges may include uncached Gamma samples; consult provider usage for billing.`;}
function showSources(){const host=$('sources');host.replaceChildren();for(const s of sources){const card=node('div',undefined,'source');card.append(node('h3',s.title));const a=sourceLink(s.url);if(a)card.append(a);card.append(node('p','Session '+s.sessionDate+' · checked '+new Date(s.capturedAt).toLocaleString(),'muted'));const details=node('details');details.append(node('summary','Inspect returned data'));details.append(node('pre',JSON.stringify(s.data,null,2)));card.append(details);host.append(card);}}
function chartViewer(source){const dialog=node('dialog',undefined,'chart-viewer'),close=node('button','Close chart'),img=node('img');img.src=source.image;img.alt=source.title;close.addEventListener('click',()=>dialog.close());dialog.append(close,node('h2',source.title),node('p','Captured '+new Date(source.capturedAt).toLocaleString()+' · assigned session '+source.sessionDate),img);if(source.captureNote)dialog.append(node('p',source.captureNote,'muted'));const link=sourceLink(source.url);if(link)dialog.append(link);dialog.addEventListener('close',()=>dialog.remove());document.body.append(dialog);dialog.showModal();}
function chartButton(source){const button=node('button','Open captured chart');button.addEventListener('click',()=>chartViewer(source));return button;}
function showCharts(){const host=$('charts');host.replaceChildren();for(const c of charts){const card=node('div',undefined,'chart-card'),remove=node('button','Remove');remove.addEventListener('click',()=>{if(busy)return;charts=charts.filter(x=>x.id!==c.id);if(c.id==='es-snapshot'){$('es-preview').hidden=true;$('basis').value='';$('basis-result').textContent='ES screenshot removed. Paste a new chart.';saveESDraft(null).catch(()=>{});}showCharts();markOld();});card.append(remove,node('h3',c.title));const img=node('img');img.src=c.image;img.alt=c.title;card.append(img,chartButton(c),node('p','Assigned session '+c.sessionDate+' · captured/attached '+new Date(c.capturedAt).toLocaleString(),'muted'));const a=sourceLink(c.url);if(a)card.append(a);host.append(card);}}
async function openPanel(panel){const source=read.sources.find(s=>s.id===panel.sourceId);if(!source?.image)return;try{const image=new Image();image.src=source.image;await image.decode();const r=panel.locationVerified?panel.region:{x:0,y:0,width:1,height:1},canvas=document.createElement('canvas');canvas.width=Math.round(r.width*image.naturalWidth);canvas.height=Math.round(r.height*image.naturalHeight);canvas.getContext('2d').drawImage(image,r.x*image.naturalWidth,r.y*image.naturalHeight,r.width*image.naturalWidth,r.height*image.naturalHeight,0,0,canvas.width,canvas.height);chartViewer({...source,title:panel.title+' ('+panel.instrument+')',captureNote:panel.locationVerified?(panel.captureComplete?'Original panel · browser-verified boundaries':'Original panel · partially visible in the captured dashboard'):'Full original chart · exact panel boundaries were not captured',image:canvas.toDataURL('image/jpeg',.95)});}catch{message('Unable to open this panel. Open its full captured chart instead.');}}
function panelButton(panel){const b=node('button','Open '+panel.title);b.addEventListener('click',()=>openPanel(panel));return b;}
function renderPanels(){renderEvidence(read,{panelButton,chartButton,onLevel:explain});}
for(const id of ['evidence','levels'])$('tab-'+id).addEventListener('click',()=>showEvidenceTab(id));
function markOld(){revision++;if(read){message('Inputs changed. The previous analysis remains below; Update & analyze will use your new chart.');$('read-state').textContent='Inputs changed';$('read-time').textContent='Previous read below · analyze again to apply current inputs.';}}
function valid(){if(!validDate($('session').value)||!$('session').reportValidity()){message('Select the chart’s session date before adding or analyzing it.');return false;}if($('instrument').value==='ES'&&($('basis').value!==''&&!$('basis').reportValidity())){message('Enter a verified ES minus SPX difference, or leave it empty to use native ES chart levels.');return false;}rememberSession();return true;}
async function provider(name,date,extra={}){const response=await fetch('/api/check?'+new URLSearchParams({provider:name,date,...extra}),{method:'POST',headers:{'X-GreeksDesk-Action':'manual-check'}});if(!response.ok)throw Error('Data request failed. Check your sign-in.');const result=await response.json();if(!result.ok||!result.count)throw Error(result.message);return result;}
function captureFrame(){if(!share)return;const image=share.frame(),record={id:'shared-chart',title:$('chart-title').value.trim()||'Shared chart',url:$('chart-url').value,sessionDate:$('session').value,capturedAt:new Date().toISOString(),image};charts=charts.filter(c=>c.id!==record.id);if(charts.length>=4)throw Error('Remove an attached chart to make room for the shared chart.');charts.push(record);showCharts();}
async function captureConnected(){
 charts=charts.filter(c=>!c.id.startsWith('connected-'));showCharts();
 const result=await connectorRequest('capture');if(!result?.ok)throw Error(result?.message||'Chart capture failed.');
 if(!Array.isArray(result.charts)||!result.charts.length||result.charts.length>4)throw Error('Connect at least one supported chart tab.');
 if(charts.length+result.charts.length>4)throw Error('Remove manually attached charts to make room for the connected charts.');
 const fresh=[];for(const c of result.charts){if(!/^data:image\/jpeg;base64,[A-Za-z0-9+/=]+$/.test(c.image)||c.image.length>6000000)throw Error('Invalid chart capture.');const bytes=Uint8Array.from(atob(c.image.split(',')[1]),ch=>ch.charCodeAt(0));fresh.push({...c,sessionDate:$('session').value,image:await resizeChart(new Blob([bytes],{type:'image/jpeg'}))});}
 charts.push(...fresh);showCharts();$('connector-status').textContent=fresh.length+' charts captured at '+new Date().toLocaleTimeString();
}
async function updateData(){
 await connectCharts(true);
 const date=$('session').value;message('Updating market data…');cycles++;usage();
 // Start a fresh source set: an unsuccessful provider is never silently reused.
 sources=[];lastUpdateWarnings=[];showSources();markOld();
 const results=await Promise.allSettled([provider('quantdata',date),provider('optionsdepth',date),provider('quantdata-context',date)]);let failure=[];
 if(results[2].status==='fulfilled'){sources.push(...results[2].value.sources);for(const item of results[2].value.sources){if(item.data.available===false)failure.push(item.title+': '+item.data.message);if(item.data.intervals?.available===false)failure.push(item.title+' intervals: '+item.data.intervals.message);}}
 const drift=results[0].status==='fulfilled'?results[0].value:null,slots=results[1].status==='fulfilled'?results[1].value:null;
 for(const r of results)if(r.status==='rejected')failure.push(r.reason.message);
 if(drift){sources.push({id:'quantdata',title:'Quant Data · SPX Net Drift',sessionDate:date,capturedAt:drift.checkedAt,url:links.quantdata,data:{...drift,priceObservations:undefined}});$('price').textContent=drift.latestPrice?.toLocaleString()??'Unavailable';$('price-time').textContent='Data: '+(drift.latestTimestamp||'No timestamp');$('data-session').textContent=date;$('data-freshness').textContent=date===today()?'Compare the source time with the current time.':'Historical session · not current market data';}
 else{$('price').textContent='Unavailable';$('price-time').textContent='Latest update failed';}
 if(slots)sources.push({id:'timestamps',title:'OptionsDepth · Available model times',sessionDate:date,capturedAt:slots.checkedAt,url:links.gamma,data:{count:slots.count,earliest:slots.slots[0],latest:slots.slots.at(-1),note:'Availability only; not an exposure reading.'}});
 if($('include-gamma').checked){
  if(!slots||!Number.isFinite(drift?.latestPrice))failure.push('Gamma needs a price reference and model timestamps.');
  else {try{const slot=[...slots.slots].filter(s=>s.startsWith(date+'T')).sort().at(-1);if(!slot)throw Error('No matching model timestamp.');message('Fetching the selected paid Gamma sample…');const gamma=await provider('optionsdepth-gamma',date,{slot,min:String(Math.floor(drift.latestPrice-60)),max:String(Math.ceil(drift.latestPrice+60))});sources.push({id:'gamma',title:'OptionsDepth · SPX Gamma heatmap',sessionDate:date,capturedAt:gamma.checkedAt,url:links.gamma,data:gamma});}catch(e){failure.push(e.message);}}
 }
 try{if(connectorEnabled){message('Capturing connected charts…');await captureConnected();}else captureFrame();}catch(e){failure.push(e.message);}
 lastUpdateWarnings=[...failure];showSources();showPrice();if(failure.length){message('Update incomplete: '+failure.join(' '));return false;}message('Data updated. Analyze when you are ready.');return true;
}
function explain(level){showEvidenceTab('levels');for(const b of document.querySelectorAll('[data-level-id]'))b.classList.toggle('selected',b.dataset.levelId===level.id);const identity=levelDetails(level,read.result.analysis);$('explanation-title').textContent=identity.name+' · '+level.price.toLocaleString()+' '+read.instrument;const host=$('explanation-body');host.replaceChildren();host.append(node('strong',identity.badge),node('p',identity.description),...(identity.sourceLabel?[node('p','Source name: '+identity.sourceLabel)]:[]),node('h3','How this level was identified'),node('p',identity.derivation));host.append(node('p',({structure:'Source: price structure',last_price:'Source: last observed quote',model_reference:'Source: model reference'})[level.role]||'Source evidence','eyebrow'),node('p',level.evidence),node('h3','Watch next'),node('p',level.watch),node('h3','What would weaken this interpretation?'),node('p',level.invalidation));for(const panelId of level.panelIds||[]){const p=read.result.analysis.panels?.find(p=>p.id===panelId);if(p)host.append(panelButton(p));}for(const id of level.sourceIds){const source=read.sources.find(s=>s.id===id);if(!source)continue;host.append(node('h3',source.title));const explanation=read.result.analysis.sources.find(s=>s.id===id);if(explanation){host.append(node('p',explanation.shows),node('p','Look for: '+explanation.lookFor));}if(source.image){const img=node('img',undefined,'evidence-image');img.src=source.image;img.alt=source.title;host.append(img,chartButton(source));}const a=sourceLink(source.url);if(a)host.append(a);} $('explanation').scrollIntoView({behavior:'smooth',block:'nearest'});}
function selectScenario(s){const card=$('scenario-'+s.direction);for(const c of document.querySelectorAll('.scenario'))c.classList.toggle('selected',c===card);card?.focus({preventScroll:true});card?.scrollIntoView({behavior:'smooth',block:'nearest'});}
function renderRead(){compactES(true);$('data-session').textContent=read.date;$('data-freshness').textContent=read.date===today()?'Check each source observation time below.':'Historical session · not current market data';showPrice(read.sources,read.sources,read.instrument);renderPanels();const a=read.result.analysis;$('map-basis').textContent=read.instrument==='ES'?(Number.isFinite(read.basis)?'Basis used for this read: ES − SPX = '+read.basis+' points.':'Native ES levels · no ES–SPX conversion applied.'):'SPX coordinates';$('headline').textContent=a.headline;$('summary').textContent=a.summary;$('map-unit').textContent=read.instrument;$('read-state').textContent=read.date===today()?'Conditional read':'Historical read';$('read-time').textContent='Analyzed '+new Date(read.result.checkedAt).toLocaleString();renderMap($('map'),a,read.instrument,explain,selectScenario);$('level-list').replaceChildren();
 $('scenarios').replaceChildren();for(const s of a.scenarios){
  const card=node('div',undefined,'scenario '+s.direction);card.id='scenario-'+s.direction;card.tabIndex=-1;
  const room=setupRoom(s,a.levels);
  card.append(node('strong',directionTitle(s.direction)),node('p',room.text,'setup-room'),node('p',s.status==='insufficient'?'Not established · '+s.condition:s.condition));
  if(s.rationale)card.append(node('p',s.rationale,'scenario-reason'));
  card.append(node('p','Confirmation: '+s.confirmation),node('p','Invalidation: '+s.invalidation));
  for(const id of [s.triggerId,s.targetId]){const l=a.levels.find(l=>l.id===id);if(l){const b=node('button',(id===s.triggerId?'Trigger ':'Destination ')+priceText(l.price)+' · '+levelDetails(l,a).name);b.addEventListener('click',()=>explain(l));card.append(b);}}
  if(s.drivers?.length){card.append(node('h3','Evidence behind this path'));for(const d of s.drivers){const p=a.panels?.find(p=>p.id===d.panelId),source=read.sources.find(src=>src.id===d.sourceId),button=node('button',(p?.title||source?.title||'Source')+' · '+d.effect,'driver-link');button.title=d.reason;button.addEventListener('click',()=>focusEvidence(d.panelId||d.sourceId));card.append(button);}}
  $('scenarios').append(card);
 }for(const id of ['changes','gaps']){$(id).replaceChildren();for(const item of a[id])$(id).append(node('li',item));} $('export').disabled=false;$('explanation-title').textContent='Why this level matters';$('explanation-body').textContent='Select a level to see its source evidence.';}
function adoptSavedRead(record){
 read=structuredClone(record);
 if(read.result.analysis.levels.some(l=>!l.role)){
  for(const scenario of read.result.analysis.scenarios){scenario.status='insufficient';scenario.triggerId=null;scenario.targetId=null;scenario.condition='This saved read predates structural-level validation. Update & analyze to rebuild this scenario.';scenario.confirmation='Refresh the analysis before using its conditional paths.';scenario.invalidation='This archived setup has not passed the current checks.';}
  read.result.analysis.gaps.push('Older saved read: conditional paths are withheld until rebuilt with current source checks.');
 }
 renderRead();
}
async function refreshHistory(restore=false){try{const records=await listReads();$('history').replaceChildren();for(const record of records){const button=node('button',record.date+' · '+new Date(record.id).toLocaleTimeString(), 'history-item');button.addEventListener('click',()=>{if(busy)return;loop.stop('Opened a saved read. Auto off.');adoptSavedRead(record);message('Viewing saved read from '+new Date(record.id).toLocaleString()+'. Current source controls are unchanged.');});$('history').append(button);}if(restore&&!read&&!busy){const saved=records.find(r=>r.date===$('session').value&&r.instrument===$('instrument').value);if(saved){adoptSavedRead(saved);$('read-state').textContent='Saved '+(saved.date===today()?'read':'historical read');message('Your saved '+saved.instrument+' map is restored. Update & analyze refreshes the evidence; Auto remains off.');}}}catch{message('Browser history is unavailable. You can still download the current read.');}}
async function analyzeEvidence(){
 const date=$('session').value,instrument=$('instrument').value;
 const selected=[...sources,...charts].filter(s=>s.sessionDate===date&&s.id!=='timestamps');
 if(!selected.length){message('Update market data or attach a chart before analyzing.');return false;}
 const version=revision,packet={date,instrument,basis:instrument==='ES'?($('basis').value===''?null:Number($('basis').value)):0,sources:selected,previous:read?.date===date&&read.instrument===instrument?read.result.analysis.summary:'',previousEvidence:read?.date===date&&read.instrument===instrument?{date,checkedAt:read.result.checkedAt,sources:read.sources.filter(s=>s.data).map(s=>({id:s.id,data:s.data}))}:null};
 const started=Date.now(),progress='Analyzing '+selected.filter(s=>s.image).length+' chart image(s) and '+selected.filter(s=>s.data).length+' data source(s) for '+date;analyses++;usage();message(progress+'…');const progressTimer=setInterval(()=>message(progress+' · '+Math.floor((Date.now()-started)/1000)+' seconds'),1000);
 try{
  const response=await fetch('/api/analyze',{method:'POST',signal:AbortSignal.timeout(160000),headers:{'Content-Type':'application/json','X-GreeksDesk-Action':'manual-check'},body:JSON.stringify(packet)});
  clearInterval(progressTimer);if(!response.ok)throw Error('Analysis request failed. Check chart size, dates and sign-in.');const result=await response.json();
  if(!result.ok)throw Error(result.message);if(version!==revision){message('Inputs changed during analysis. The result was not applied.');return false;}
  for(const warning of lastUpdateWarnings)result.analysis.gaps.push('Provider update: '+warning);read={id:new Date().toISOString(),date,instrument,basis:packet.basis,sources:structuredClone(selected),result};renderRead();if(!loop.status().active)$('scenario-section').scrollIntoView({behavior:'smooth',block:'start'});
  try{await saveRead(read);await refreshHistory();}catch{message('Analysis ready. Local history could not be saved; download this read.');return true;}
  message(result.model==='source-reference-summary'?'Source references ready. Add a chart to develop scenarios; no OpenAI request was needed.':(result.analysis.levels.length?lastUpdateWarnings.length?'Analysis ready with a provider gap. See What changed & what needs checking.':'Analysis ready. Click a level to inspect the original evidence.':'Chart review complete. No supported '+instrument+' levels yet; see the panel findings and missing evidence below.'));return true;
 }catch(error){clearInterval(progressTimer);message(error.message);if(read){$('read-state').textContent='Previous read only';$('read-time').textContent='Latest analysis failed · previous analysis remains visible.';}return false;}
}
const loop=createUpdateLoop({run:async()=>{if(busy||!valid())return false;if(loop.status().active&&$('session').value!==today()){message('Session date changed at midnight. Auto stopped.');return false;}lock(true);try{const mode=manualMode,ok=await updateData();if((mode==='analysis'&&(ok||charts.length))||(mode===null&&ok&&$('auto-analysis').checked))return await analyzeEvidence();return ok;}catch(e){message(e.message);return false;}finally{lock(false);}},notify:s=>{$('auto').textContent=s.active?'Auto: ON · Stop':'Auto: OFF';$('auto').setAttribute('aria-pressed',String(s.active));$('interval').disabled=s.active;$('duration').disabled=s.active;if(!s.busy&&s.active)message(s.message+' · '+s.remaining+' cycles left');}});
async function manualUpdate(mode){if(busy||!valid())return;manualMode=mode;try{await loop.once();}finally{manualMode=null;}}
$('update').addEventListener('click',()=>manualUpdate('data'));
$('analyze').addEventListener('click',()=>manualUpdate('analysis'));
$('auto').addEventListener('click',()=>{if(loop.status().active){loop.stop();message('Auto updates stopped.');return;}if(busy||!valid())return;if($('session').value!==today()){message('Auto updates require today’s New York date. Use manual updates for historical sessions.');return;}const delay=Number($('interval').value)*60000,total=Number($('duration').value)*60000;loop.start({intervalMs:delay,durationMs:total,maxCycles:Math.ceil(total/delay)});});
for(const id of ['session','instrument','basis'])$(id).addEventListener('change',()=>{loop.stop('Inputs changed.');markOld();$('basis-label').hidden=$('instrument').value!=='ES';if(id==='session'){compactES(false);rememberSession();contextLabel();$('confirm-es').checked=false;$('es-price-time').value='';$('es-preview').hidden=true;$('es-preview').removeAttribute('src');$('basis-result').textContent='Session changed. Paste an ES chart for '+$('session').value+'.';$('basis').value='';basisProposal=null;$('apply-basis').hidden=true;sources=[];charts=[];showSources();showCharts();$('price').textContent='—';$('data-session').textContent='—';}showPrice();message('Settings changed. Update and analyze for these inputs.');});
// Keep a bounded auto session running when the trader switches to another tab or app.
// The loop checks its wall-clock deadline before every request, including delayed background timers.
$('share').addEventListener('click',async()=>{if(!valid())return;try{share?.stop();share=await startChartShare();$('stop-share').hidden=false;$('capture-status').textContent='Sharing active. Update data captures a fresh frame of the selected surface.';share.stream.getVideoTracks()[0].addEventListener('ended',()=>{share=null;$('stop-share').hidden=true;loop.stop();$('capture-status').textContent='Sharing ended. Existing image is a saved capture.';});captureFrame();markOld();}catch(e){message(e.message);}});
$('stop-share').addEventListener('click',()=>{share?.stop();share=null;$('stop-share').hidden=true;loop.stop();$('capture-status').textContent='Sharing stopped. Existing image is a saved capture.';});
async function attach(files){if(busy||!valid())return;for(const file of files){if(charts.length>=4){message('Up to four charts per read. Remove one before adding another.');break;}try{const image=await resizeChart(file);charts.push({id:'chart-'+crypto.randomUUID().slice(0,8),title:$('chart-title').value.trim()||file.name,url:$('chart-url').value,sessionDate:$('session').value,capturedAt:new Date().toISOString(),image});}catch(e){message(e.message);}}showCharts();markOld();}
$('upload').addEventListener('change',async e=>{await attach(e.target.files);e.target.value='';});
document.addEventListener('paste',e=>{const files=[...(e.clipboardData?.files||[])];if(files.length){e.preventDefault();if(e.target.closest?.('#es-drop'))inspectES(files[0]);else attach(files);}});
$('export').addEventListener('click',()=>{if(!read)return;const url=URL.createObjectURL(new Blob([JSON.stringify(read,null,2)],{type:'application/json'})),a=node('a');a.href=url;a.download='greeksdesk-'+read.date+'.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000);});
window.addEventListener('pagehide',()=>{loop.stop();share?.stop();});
refreshHistory(true);

fetch('/api/config').then(r=>r.json()).then(c=>{$('configuration').textContent=c.analysisConfigured?'Analysis connected. Selected data and images are sent to OpenAI only when analysis is requested.':'Add OPENAI_API_KEY in Fly secrets to enable analysis. Data updates and local charts still work.';}).catch(()=>{$('configuration').textContent='Unable to check analysis connection.';});

async function connectCharts(silent=false){try{const result=await connectorRequest('status');if(!result?.ok)throw Error(result?.message||'Connector unavailable.');connectorEnabled=result.count>0;$('connector-status').textContent=connectorEnabled?result.count+' chart tabs connected. Update & analyze will capture them.':'Connector installed. Select chart tabs in its toolbar popup.';$('connect-charts').textContent=connectorEnabled?'Reconnect charts':'Connect charts';if(!silent)message($('connector-status').textContent);}catch(error){connectorEnabled=false;$('connector-status').textContent='One-time browser connector setup needed.';if(!silent)message(error.message+' Open the connector setup guide below.');}}
$('connect-charts').addEventListener('click',()=>connectCharts());
connectCharts(true);

let basisProposal=null;
function marketTime(value){const time=new Date(value);return Number.isFinite(time.getTime())?new Intl.DateTimeFormat('en-US',{timeZone:'America/New_York',month:'short',day:'numeric',hour:'numeric',minute:'2-digit',timeZoneName:'short'}).format(time):'time not established';}
function compactES(compact){$('es-drop').classList.toggle('compact',compact);$('toggle-es').hidden=false;$('toggle-es').textContent=compact?'Show chart & details':'Collapse chart';$('toggle-es').setAttribute('aria-expanded',String(!compact));}
$('toggle-es').addEventListener('click',()=>compactES(!$('es-drop').classList.contains('compact')));

function chartContext(){return $('confirm-es').checked?{instrument:'ES',sessionDate:$('session').value,priceTime:$('es-price-time').value||null,timezone:'America/New_York'}:null;}
function contextLabel(){$('confirm-es-label').textContent='This is my ES price chart for '+$('session').value;}
function showPrice(selected=charts,selectedSources=sources,instrument=$('instrument').value){
 const es=selected.find(c=>c.id==='es-snapshot')?.observation,spx=selectedSources.find(s=>s.id==='quantdata')?.data;
 $('price-label').textContent=instrument==='ES'?'Chart price · ES':'API reference · SPX';
 if(instrument==='ES'){$('price').textContent=Number.isFinite(es?.price)&&es.instrument==='ES'?es.price.toLocaleString(undefined,{minimumFractionDigits:2}):'—';$('price-time').textContent=es?.instrument==='ES'?'Snapshot · '+(marketTime(es.timestamp)):'Read your pasted ES chart';}
 else{$('price').textContent=spx?.latestPrice?.toLocaleString()??'—';$('price-time').textContent=spx?.latestTimestamp?'SPX source: '+marketTime(spx.latestTimestamp):'No SPX reading';}
}
contextLabel();
for(const id of ['confirm-es','es-price-time'])$(id).addEventListener('change',()=>{
 if(busy)return;const chart=charts.find(c=>c.id==='es-snapshot');$('basis').value='';basisProposal=null;
 if(chart){chart.confirmedContext=chartContext();delete chart.observation;saveESDraft(chart).catch(()=>{});}
 showPrice();markOld();$('basis-result').textContent=chartContext()?'ES and session confirmed by you. Analyze these charts to build native ES levels; read the chart again to check its latest price and basis.':'Chart confirmation removed. The image must show its instrument and date.';
});
$('analyze-es').addEventListener('click',async()=>{if(busy||!valid())return;lock(true);loop.stop();try{await analyzeEvidence();}finally{lock(false);}});

async function inspectES(file,retry=false){if(busy||!file)return;if(!valid()){$('basis-result').textContent='Choose the chart session date at the top before pasting the ES screenshot.';return;}compactES(false);if(!retry){$('confirm-es').checked=false;$('es-price-time').value='';}lock(true);loop.stop();basisProposal=null;$('basis').value='';markOld();$('apply-basis').hidden=true;$('basis-result').textContent='Reading ES screenshot and comparing SPX time...';try{const image=await resizeChart(file);$('es-preview').src=image;$('es-preview').hidden=false;if(charts.filter(c=>c.id!=='es-snapshot').length>=4)throw Error('Remove a chart to make room for ES.');charts=charts.filter(c=>c.id!=='es-snapshot');charts.push({id:'es-snapshot',title:'DeepCharts ES snapshot',sessionDate:$('session').value,capturedAt:new Date().toISOString(),image,confirmedContext:chartContext()});showPrice();showCharts();markOld();await saveESDraft(charts.find(c=>c.id==='es-snapshot')).catch(()=>message('ES image attached; local saving is unavailable.'));$('basis').value='';const response=await fetch('/api/basis',{method:'POST',signal:AbortSignal.timeout(100000),headers:{'Content-Type':'application/json','X-GreeksDesk-Action':'manual-check'},body:JSON.stringify({date:$('session').value,image,confirmedContext:chartContext()})});const r=await response.json();if(r.imageType==='price_chart'&&r.observed?.instrument==='ES'&&Number.isFinite(r.observed.price)&&r.observed.price>0){const chart=charts.find(c=>c.id==='es-snapshot');chart.observation=r.observed;await saveESDraft(chart).catch(()=>{});showPrice();}if(!r.ok&&['chart_thumbnail','other'].includes(r.imageType)){$('basis-result').textContent='No basis calculated. '+r.message;message('The saved image is not a readable original ES chart. Paste a new DeepCharts screenshot.');return;}if(!r.ok){const o=r.observed;if(r.imageType==='price_chart'&&o?.instrument==='ES'&&Number.isFinite(o.price)){$('basis-result').textContent='ES chart read: '+o.price.toLocaleString(undefined,{minimumFractionDigits:2})+'. No SPX conversion applied. '+r.message;message('Your ES chart is ready. Choose Analyze these charts for native ES levels.');return;}throw Error(r.message+(o?' Read from image: '+o.instrument+' / price '+(o.price??'unreadable')+' / time '+o.timestamp+'.':''));}basisProposal={...r,date:$('session').value,image};$('basis-result').textContent='ES minus SPX: '+r.basis+' = ES '+r.esPrice+' ('+r.esTime+') minus SPX '+r.spxPrice+' ('+r.spxTime+'). Contract: '+r.contract+'. Applied automatically; check the extracted prices.';$('basis').value=r.basis;$('instrument').value='ES';$('basis-label').hidden=false;loop.stop();markOld();message('ES basis '+r.basis+' applied from the displayed observations.');}catch(e){$('basis-result').textContent='Basis unavailable: '+e.message+' The ES image remains available for native ES analysis; SPX conversion is disabled.';}finally{lock(false);}}
$('es-image').addEventListener('change',e=>inspectES(e.target.files[0]));
$('es-drop').addEventListener('dragover',e=>e.preventDefault());$('es-drop').addEventListener('drop',e=>{e.preventDefault();inspectES(e.dataTransfer.files[0]);});
$('apply-basis').addEventListener('click',()=>{if(!basisProposal||busy||basisProposal.date!==$('session').value)return;charts=charts.filter(c=>c.id!=='es-snapshot');if(charts.length>=4){message('Remove one chart before adding the ES snapshot.');return;}charts.push({id:'es-snapshot',title:'DeepCharts ES snapshot',sessionDate:basisProposal.date,capturedAt:new Date().toISOString(),image:basisProposal.image});showCharts();$('basis').value=basisProposal.basis;$('instrument').value='ES';$('basis-label').hidden=false;loop.stop();markOld();$('apply-basis').hidden=true;message('Basis applied from the displayed observations. It can change; refresh it for a later session.');});

$('paste-es').addEventListener('click',()=>{compactES(false);$('paste-es').focus();});

$('retry-basis').addEventListener('click',async()=>{if(busy)return;const chart=charts.find(c=>c.id==='es-snapshot');if(!chart){$('basis-result').textContent='Paste your ES screenshot in the box above first.';return;}try{await inspectES(chartImageBlob(chart.image),true);}catch(error){$('basis-result').textContent=error.message;}});
loadESDraft($('session').value).then(draft=>{if(!draft||charts.some(c=>c.id==='es-snapshot')||draft.sessionDate!==$('session').value)return;charts.push({...draft,id:'es-snapshot'});$('es-preview').src=draft.image;$('es-preview').hidden=false;showCharts();$('confirm-es').checked=!!draft.confirmedContext;$('es-price-time').value=draft.confirmedContext?.priceTime||'';showPrice();$('basis-result').textContent=draft.observation?.instrument==='ES'?'Saved ES chart · '+draft.observation.price.toLocaleString(undefined,{minimumFractionDigits:2})+' at '+marketTime(draft.observation.timestamp)+'. Native ES analysis is available. Read again only to refresh the price/basis check.':'Saved ES screenshot restored for '+draft.sessionDate+'. Read the chart or analyze native ES levels.';}).catch(()=>{});
